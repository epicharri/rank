#pragma once

namespace epic
{
  namespace kind
  {
    enum
    {
      poppy = 1,
      cum_poppy = 2,
      flat_poppy = 3,
      max_warp_size = 64,
      one_zero_and_then_all_ones_bit_vector = 1,
      random_bit_vector = 2,
      from_file_bit_vector = 3,
      write_only = true,
      not_write_only = false
    };

  }
}